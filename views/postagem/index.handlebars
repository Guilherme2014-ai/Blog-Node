<div class="card">
    <div class="card-body">
        <h1> {{post.titulo}} </h1>
        <hr>
        <p>{{post.conteudo}}</p>
        <hr>
        <small>Data de publicação: {{post.data}}</small>
        <hr>
        <p> {{post.conteudo}} </p>
    </div>
</div>
